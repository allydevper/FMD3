{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "cf-bypass",
  "description": "Capability for the embedded Cloudflare-bypass window. Loads untrusted remote content; must NOT inherit main's IPC permissions.",
  "windows": ["cf-bypass"],
  "permissions": []
}
